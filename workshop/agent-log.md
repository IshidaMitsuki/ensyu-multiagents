# Agent Log

このファイルは、各エージェントの共有メモリです。Copilot Chatで役割プロンプトを実行したら、結果をこの下に追記してください。

## Initial State

- アプリ名: 研究タスクボード
- 現在の機能: タスク追加、状態変更、削除、ローカル保存
- 演習の目的: 役割分担したAI開発の流れを体験する

## 2026-05-11

- ドキュメントの役割呼び出し先を `.github/prompts/` から `.github/agents/` に揃えた
- README とワークシートの表記を custom agents 前提に更新した
- 受講者はプロンプトファイルではなく、各役割の `.agent.md` を Copilot Chat から使う

## 2026-05-11 追記

- Planner → Implementer → Tester → Reviewer → Documenter を人間が順番に実行する方式に整理した
- README とワークシートから、自動一括実行を想定した操作手順を削除した
- カスタムエージェントが表示されない場合は、対象の `.agent.md` ファイル本文をCopilot Chatに貼り付けて進める説明を追加した

## 2026-05-12 Documenter追記

- 研究室演習の導入資料として `workshop/multi-agent-intro.md` を追加した
- `workshop/multi-agent-intro.pptx` を生成するための `workshop/create-multi-agent-intro-ppt.ps1` を追加した
- README のフォルダ構成に Markdown 版と PowerPoint 版の導入資料を追記した
- 追加コンテンツ: マルチエージェントの意味、5つの役割、共有ログ、人間の判断ポイント、演習で体験する流れ

## 2026-05-13 Planner

- 提案する改善: タスク名で絞り込める検索欄を追加する
- ユーザーストーリー: 研究タスクが増えても、キーワードを入れるだけで目的のタスクをすぐ見つけたい
- 受け入れ条件:
	- 検索欄に文字を入力すると、タスク名にその文字を含むタスクだけが一覧に表示される
	- 検索語を消すと、すべてのタスクが元どおり表示される
	- 検索中に一致するタスクが0件なら、各列に「検索結果がありません」と分かる表示が出る
- 追加してよいコンテンツ: 検索欄のプレースホルダー文言、空表示の文言、必要ならサンプルタスクを1件だけ検索しやすい語に調整する
- 変更対象ファイル: app/index.html, app/styles.css, app/app.js
- 実装順序:
	1. `app/index.html` に検索欄を追加する
	2. `app/app.js` に検索状態と絞り込み処理を入れる
	3. `app/styles.css` で検索欄と空表示の見た目を整える
	4. ブラウザで検索、解除、空表示を確認する
- リスク: 表示がフィルタ後の件数に変わるため、空状態の見え方を先に決めないと受講者が迷いやすい
- Implementerへの依頼文: 共有ログのこの計画に従って、タスク名検索を最小実装で追加してください。既存の追加・状態変更・削除・保存を壊さず、実装後は差分と確認結果を追記してください。

## 2026-05-13 Implementer

- 変更したファイル: app/index.html, app/styles.css, app/app.js
- 実装内容: タスク名を部分一致で絞り込める検索欄を追加し、検索中は一致しない列に「検索結果がありません」を表示するようにした
- 手動確認: 検索欄に「関連」と入れると該当タスクだけが残り、検索語を消すと元の2件表示に戻ることを確認した
- 手動確認: 検索欄に「zzz」と入れると3列すべてが「検索結果がありません」になることを確認した
- 追加したコンテンツ: 検索欄のプレースホルダー文言と、検索時の空表示文言を追加した
- 今後の案: 検索対象をタイトル以外にも広げる案はあるが、今回は実装しない
- Testerへの依頼: 検索欄の入力・解除、0件検索、既存の追加・状態変更・削除・保存が壊れていないかをブラウザで確認してください

## 2026-05-13 Tester

- テスト観点: 検索の絞り込み、空文字の扱い、長い文字列の表示、削除と再読み込みによる保存、スマホ幅でのレイアウト崩れを確認する
- テストケース:
	1. 検索欄に「関連」を入力すると、進行中の1件だけが表示され、検索語を消すと元の一覧に戻る
	2. タスク名を空欄のまま「追加」を押しても、新しいタスクは増えない
	3. 長いタスク名を追加すると一覧に表示される。長文の折り返しによる見た目崩れは未確認
	4. タスクの状態変更と削除を行ったあとに再読み込みすると、件数と状態が保存されたまま残る
	5. スマホ幅(375px)では、検索欄・追加欄・3列のボードが縦に並び、横にはみ出していない
- 追加したテスト用コンテンツ: 長文確認用の一時タスクを1件追加したが、確認後に削除した
- 見つけた問題: 重大な問題は見つからなかった
- 修正した場合の変更ファイル: なし
- 未確認事項: 長文タスクのモバイル表示、検索語に空白だけを入れた場合の見え方、入力欄の最大文字数ちょうどでの挙動
- Reviewerに渡すレビュー依頼: 検索追加後も既存の追加・状態変更・削除・保存が壊れていないか、また検索時の空表示が受講者に分かりやすいかを確認してください
